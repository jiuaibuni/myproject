server:
  port: 80

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/myproject?useSSL=false&useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: bing

  redis:
     database: 0
     host: 127.0.0.1
     port: 6379
     lettuce:
       pool:
         max-idle: 20
         min-idle: 500
         max-active: 2000
         max-wait: -1
